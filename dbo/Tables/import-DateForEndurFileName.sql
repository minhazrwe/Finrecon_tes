CREATE TABLE [dbo].[import-DateForEndurFileName] (
    [Date] NVARCHAR (10) NULL,
    [ID]   INT           IDENTITY (1, 1) NOT NULL,
    CONSTRAINT [pk_DateForEndurFileName] PRIMARY KEY CLUSTERED ([ID] ASC)
);


GO

